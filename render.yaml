services:
  - type: web
    name: studiox-cloud
    env: python
    buildCommand: bash -lc "cd frontend && npm ci && npm run build && cd .. && pip install --upgrade pip && pip install -r requirements.txt"
    startCommand: python app.py
    plan: free
    autoDeploy: true
